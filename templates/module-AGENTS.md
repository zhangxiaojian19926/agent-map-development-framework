# AGENTS.md — <module-id>

> 公共模板,遵循 [公共协作](../docs/framework/collaboration-policy.md) 与 [知识规则](../docs/framework/knowledge-policy.md)。实例信息从项目资源登记取得。生成到实际模块时必须按模块目录深度重算链接,不得原样复制模板路径。
>
> 模板:生成时替换所有占位符,未知事实明确标注待核实;不复制未经核实的命令。本文不是已注册模块。

## 用途与上级

- 模块 ID、模块根、Git 归属。
- 负责什么、不负责什么;顶层流程、项目架构、约束、协作文档的有效相对链接。
- 目录父子与运行调用方分别说明。

## 调用与接口

- 实际启动/调用入口、工作目录、输入输出、错误与超时。
- 上游调用者、下游依赖、共享接口责任及兼容边界。

## 内部协作

按真实源码列子组件、职责、输入输出和调用顺序;链接适用子级 AGENTS.md。说明目录与源码位置不同则明确指出。

## 本模块开发流程

定位责任 → 加载局部规则 → 映射规格/计划 → 实现 → 最小验证 → 跨模块交接 → 完成报告与知识回流。
只写相对于顶层的模块差异,不复制全部技能流程。

## 命令与验证

给出已核实命令、执行目录、依赖、是否有外部副作用及成功判据。
区分 Host/Cross-build/Target/Integration,没有相应测试时如实说明。

## 知识与完成标准

声明已有 KB alias(没有则明确未关联),由项目注册表解析路径。列适用完成证据、版本/产物标识和交接要求。
