## 执行记录（2026-09-25）

本轮用户明确要求搭建一键自动化并用真实 Codex CLI 在隔离目录开发小需求，作为本轮实现和验证的授权；不是从配置或 Agent 自行声明推断批准。new 入口另行验证三份产物和当前调用的 SHA256 确认。宏观实现与证据索引见 `docs/verification/bootstrap-0.2.0.md`；微计划中的逐项提交未执行，不能把未勾选的历史微步骤当作已逐条执行日志。

6.4 部分完成：macOS/Linux 离线合成回归均 58 项通过，macOS 真实 CodeGraph/Codex 全流程通过；Linux 真实外部工具联调尚未执行，所以保留未完成。未 push、未以历史 CI 替代当前 CI，未关闭或归档本 change。

## 1. 规格批准与执行准备

- [x] 1.1 用户审阅并批准本 change 的设计及行为规格；记录批准范围和产物指纹，验证不是由文件存在或 Agent 自行批准推断（BOOT-02）。
- [x] 1.2 规格获批后用 writing-plans 生成逐文件、逐测试的微步骤计划，关联所有 Req/Scenario；计划经批准后再 apply，验证场景无遗漏。
- [x] 1.3 核实公共工作树、基线和原业务工程隔离；验证仅公共包被修改，已有 22 项测试作为回归基线通过。

## 2. CLI、配置与事务

- [x] 2.1 实现统一入口参数解析、交互向导和 JSON 输出；测试 init/new 非交互缺参、中文/空格路径及退出码（BOOT-01、READY-01）。
- [x] 2.2 实现能力计划、dry-run 和可信确认边界；先运行失败测试，再验证 dry-run 零写入、下载配置不能自授权、只读任务不写（BOOT-01-INIT、BOOT-03-CONSENT）。
- [x] 2.3 实现路径/指纹检查、原子写、互斥与事务恢复；验证用户并发修改保留、重跑幂等、取消和中断恢复（BOOT-03-CHANGED、BOOT-04-RETRY）。
- [x] 2.4 实现公共包复制及模板生成；验证技能相对引用、许可证、机器状态排除、人工 AGENTS 冲突和不重复配置（BOOT-04、MAP-03）。
- [x] 2.5 实现 new 需求/设计/计划审批门禁与本地 OpenSpec 根绑定；验证上级根不被写入、空模块不索引、不隐式 Git/远程初始化（BOOT-01-NEW、BOOT-02-ROOT、MOD-03-EMPTY）。

## 3. 模块接入

- [x] 3.1 实现有界发现与 module add，支持无 Git 描述确认；验证三模块、多深度、子仓库、越界链接及重复 ID（MOD-01、MOD-02）。
- [x] 3.2 实现受控 module clone 并复用 add 流程；验证指定版本、超时、凭据脱敏、不递归执行子模块/钩子、失败后可恢复（MOD-01-IMPORT）。
- [x] 3.3 实现 module sync、移动/missing 和稳定 ID 历史；验证只更新客观目录、不会偷偷调 Agent、不会删除旧关系（MOD-03-MOVE）。

## 4. CodeGraph 与更新机制

- [x] 4.1 审计选定 CodeGraph 版本的 init/install/status 副作用，形成兼容适配；验证禁止隐式下载/全局安装及不支持版本明确拒绝（CG-01-TOOL）。
- [x] 4.2 实现 index-id 去重、初始化/同步、代表性查询和范围报告；受控 CLI 测试通过后在合成源码上记录真实查询证据（CG-01-INDEX）。
- [x] 4.3 实现项目受管 hooks 及 worktree 感知 dispatcher；验证原 hook 保留、外置路径拒绝、事件合并、Git 不被阻断且不调用 LLM（CG-02）。
- [x] 4.4 实现 Agent 任务入口/收尾的新鲜度检查，复用监听状态；验证未提交修改和普通 clone 在下次入口被识别，不新增全局 watcher（CG-03-STALE）。

## 5. Agent 与地图

- [x] 5.1 实现 Codex CLI 能力探测和通用交接；验证缺失/损坏/未认证显示 WAITING_AGENT，正确超时取消，不使用提升权限参数（MAP-01）。
- [x] 5.2 按 writing-skills 流程建立 project-bootstrap 技能与最小上下文包；用受控决策测试验证新库审批、现有工程、未知关系和不可信指令处理（BOOT-02、MAP-04）。
- [x] 5.3 实现关系 schema、证据和源码指纹校验及设计/实际差异；测试伪接口、过期引用、不存在模块、越界输出、静态证据不能标运行通过（MAP-02、MAP-04）。
- [x] 5.4 实现模块 AGENTS、项目侧入口和地图渲染；验证任意深度链接、人工内容保护、读写范围和增量结果稳定（MAP-03）。
- [x] 5.5 在获准的独立工具位置解决真实 CLI 可用性，完成真实 Agent 对合成三模块项目建图；保留原始结果和版本，不以 stub 或当前桌面会话替代 CLI 验收（MAP-01-AGENT）。

## 6. 文档、诊断和发布验证

- [x] 6.1 实现只读 doctor 和分层状态报告；验证 no-code、混合失败、WAITING_AGENT、STALE 的退出码和恢复指引，零安装/同步/文档写入（READY-01、READY-02-READONLY）。
- [x] 6.2 更新顶层流程、公共技能/协作/知识规则、接入说明与项目模板；验证 init/new 两条教程、自动化授权例外、新旧边界一致且不重复权威事实。
- [x] 6.3 更新公共包版本、能力清单、许可证说明和分发检查；验证新文件已登记，原技能和知识脚本兼容，发布检查不混同目标工程诊断。
- [ ] 6.4 macOS/Linux 执行合成全流程、二次运行、失败恢复、hook 冲突和地图增量测试，并重跑现有回归；证据绑定最终工作树（READY-02-RELEASE）。
- [x] 6.5 完成独立评审（需可用且获授权）或明确标注自查，处理重要问题，交付真实 CodeGraph/Agent 验收及剩余限制；未经单独授权不 push、发布、迁移业务工程或摄入知识。
